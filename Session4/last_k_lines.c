#include <stdio.h>
#include <stdlib.h>
#include <files.h>

int main(){
	int total_no_of_lines = 0;
	char ch;
	FILE *fp = NULL;
	char name[100];
	
	scanf("%s",name);
	fp = fopen(name,"r");
	
	fseek()
	
}
